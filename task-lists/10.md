# Session 10 – Stream mining continued: clustering and classification on streams

*Aligned with syllabus: stream mining continued—clustering and classification methods for data streams; lab: **RapidMiner** for stream exploration.*

---

# Task 1 – Conceptual questions

Answer in your own words:

1. Why can’t we usually store an entire stream to run *k*-means **from scratch** every time?
2. What is **micro-clustering** (high-level) in stream clustering algorithms such as CluStream-style approaches?
3. How does **prequential evaluation** (test-then-train) differ from classic **k-fold** CV on streams?

---

# Task 2 – RapidMiner stream process (or documented alternative)

**Option A:** Build a **RapidMiner** process that reads a stream or uses a **Sample (bootstrapping)** / iterative file reader pattern your instructor approves, and applies **one** stream-capable operator (e.g. stream classification or clustering setup available in your version). Include:

* screenshot of the process,
* operator names and **two** non-default parameters,
* short interpretation of output.

**Option B:** If RapidMiner is not available, use **Python** (`River`, `scikit-multiflow`, or incremental `partial_fit` on mini-batches). Document:

* data source (synthetic or real),
* model name,
* evaluation protocol (prequential window size or batch size),
* **accuracy** or **purity** over stream (plot or table).

---

# Task 3 – Short comparative reflection

In **half a page**, compare **one** limitation of your stream setup with **batch** training on the **same** dataset loaded entirely into RAM.

---

# Deliverables

Students must submit:

1. **Concept answers (Task 1).**
2. **Tool documentation + results (Task 2).**
3. **Reflection (Task 3).**

**Submission format:**  
PDF report, **max. 4–5 pages**, single file.

**Naming:**  
`ADD_StreamMiningRM_LastName_FirstName.pdf`
