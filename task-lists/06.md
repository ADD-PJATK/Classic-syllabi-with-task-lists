# Session 6 – Clustering large multidimensional data (intro) and RapidMiner ↔ Hadoop context

*Aligned with syllabus: clustering algorithms for large, high-dimensional data; lab: intro to RapidMiner with Hadoop communication and **project topic discussion**.*

---

# Task 1 – Conceptual questions

Answer in your own words:

1. What is **clustering** and how does it differ from **classification**?
2. Why do **distance measures** and **curse of dimensionality** matter for clustering large feature spaces?
3. What is the **BFR algorithm** (high level) and which limitation of *k*-means in one pass over huge data does it try to address?
4. What roles do **HDFS** and **distributed processing** play when feature vectors or intermediate statistics no longer fit on one node?

---

# Task 2 – *k*-means on a small numeric dataset

Use a public **numeric** dataset (e.g. sklearn `iris` **without** using labels in the algorithm, or another **≤10** features table).

1. Describe **n**, number of features, and any scaling you applied.
2. Run **k-means** for **k = 2, 3, 4**. Report **within-cluster SSE** (sum of squared errors to centroids) or another internal criterion available in your tool.
3. Plot clusters in **2D** using the **first two PCA components** (or two chosen features) for **one** value of **k** you justify as reasonable.

---

# Task 3 – Tool exercise (pick one)

**Option A – RapidMiner:** Build a process that loads a sample table, applies **Normalize**, runs **k-Means**, and outputs a **cluster column**. Paste a **screenshot** of the process and note main operator parameters.

**Option B – Python:** `sklearn.cluster.KMeans` + short notebook-style script. Print cluster sizes and centroids (or their norms).

---

# Task 4 – Project checkpoint (planning)

Your **team** should record (in this submission or in a shared appendix your instructor accepts):

1. **Provisional project title** and **dataset** domain.
2. **Two** concrete questions you want the project to answer.
3. **Division of roles** (who prepares data, who runs experiments, who writes the report draft).

*This block is light-weight documentation; it will evolve in Session 9 and in the final project.*

---

# Deliverables

Students must submit:

1. **Concept answers (Task 1).**
2. **k-means experiments and PCA 2D plot (Task 2).**
3. **RapidMiner screenshot or Python output (Task 3).**
4. **Project planning snippet (Task 4)** – bullet list is acceptable.

**Submission format:**  
PDF report, **max. 4–5 pages**, single file.

**Naming:**  
`ADD_ClusteringIntro_LastName_FirstName.pdf`
